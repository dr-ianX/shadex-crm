services:
  - type: web
    name: shadex-backend
    env: node
    buildCommand: npm ci && npm run build
    startCommand: cd packages/backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
